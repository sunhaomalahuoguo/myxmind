package multithreadingAndHighConcurrency;

import java.util.HashMap;
import java.util.HashSet;

public class Test {

	public static void main(String[] args) {
		new Object().equals(obj)
		HashMap map = new HashMap<>();
		HashSet set = new HashSet<>();
		set.add(e)
		map.put(key, value)
		map.equals(o)
		map.hashCode()
		map.put(key, value);
	}
}
